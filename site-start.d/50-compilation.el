;;; compilation.el --- custom compilation options
;;; Commentary:
;;; Code:

;;; do not truncate compilation lines
(setq-default compilation-max-output-line-length nil)

(provide '50-compilation)
;;; 50-compilation.el ends here
